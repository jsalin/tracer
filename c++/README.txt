To compile the Windows version in Visual Studio 2019, simply open the solution and run. Remember to choose x64 release profile for best speed!

To compile in Ubuntu, install following dependencies and then build by running make command in this directory:

$ sudo apt install build-essential libsdl2-dev git
$ make
$ ./tracer
